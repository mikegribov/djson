It's distributed JSON over the file structure.
It allows you to present the same structure that is representable in JSON format as in a single json file,
and in several, distributing parts of the structure across several files and directories.